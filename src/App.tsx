import { BrowserRouter } from "react-router-dom";

import Layout from "components/common/layout";

function App() {
  return (
    <BrowserRouter>
      <Layout />
    </BrowserRouter>
  );
}

export default App;
